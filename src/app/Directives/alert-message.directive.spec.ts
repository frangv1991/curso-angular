import { AlertMessageDirective } from './alert-message.directive';

describe('AlertMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertMessageDirective();
    expect(directive).toBeTruthy();
  });
});
